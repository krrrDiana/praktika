#include <stdio.h>

int main() {
    int t1, t2, t3;
    scanf("%d %d %d", &t1, &t2, &t3);

    double total_time = t1 + t2 + t3;
    double time_in_hours = total_time / 3.0;

    printf("Час, необхідний для з'їдання: %.2f годин", time_in_hours);

    return 0;
}
